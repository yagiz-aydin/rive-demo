import React from 'react';
import ReactDOM from 'react-dom/client';
import RiveApp from './RiveApp';
import AframeApp from './AframeApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RiveApp />
  </React.StrictMode>
);
